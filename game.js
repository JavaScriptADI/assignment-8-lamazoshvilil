function cratebutton(game){
     const button = document.createElement(`button`);
     // button.addEventListener(`click` (e) => {

     // }
     button.style.width = `200 px`;
     button.style.margin = `auto`;
     button.style.height = `100px`;
}
const button = document.createElement(`button`);
